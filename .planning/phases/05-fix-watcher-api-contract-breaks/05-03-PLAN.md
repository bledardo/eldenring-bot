---
phase: 05-fix-watcher-api-contract-breaks
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - COMM-01
  - COMM-02
  - COMM-03
  - COMM-04
  - COMM-05
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - BET-03
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - STAT-06
  - STAT-07

must_haves:
  truths:
    - "All Python tests pass (watcher event payload tests)"
    - "All JavaScript tests pass (apiServer tests)"
    - "REQUIREMENTS.md checkboxes updated for all Phase 5 requirement IDs"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability with Phase 5 requirements marked complete"
      contains: "[x] **COMM-01**"
  key_links:
    - from: "watcher/watcher.py"
      to: "delu-bot/src/apiServer.js"
      via: "Watcher sends boss_canonical_name -> API reads data.boss_canonical_name"
      pattern: "boss_canonical_name"
---

<objective>
Run full test suites on both Python and JavaScript sides, verify all 6 success criteria from the phase definition, and update REQUIREMENTS.md traceability.

Purpose: Confirm the contract between Watcher and API server is fully repaired end-to-end. Update requirement tracking to reflect completion.

Output: Verified test results, updated REQUIREMENTS.md.
</objective>

<execution_context>
@/home/hamza/.claude/get-shit-done/workflows/execute-plan.md
@/home/hamza/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fix-watcher-api-contract-breaks/05-01-SUMMARY.md
@.planning/phases/05-fix-watcher-api-contract-breaks/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suites and verify all 6 phase success criteria</name>
  <files></files>
  <action>
Run both test suites and verify the 6 phase success criteria:

**Python tests:**
```bash
cd /home/hamza/eldenring-bot && python -m pytest tests/ -v
```

**JavaScript tests:**
```bash
cd /home/hamza/eldenring-bot/delu-bot && npx vitest run
```

**Verify 6 success criteria by code inspection:**

1. **Watcher sends boss_canonical_name:** `grep "boss_canonical_name" watcher/watcher.py` — present in all 4 callbacks + session_start.
2. **Every event includes event_id:** `grep "event_id" watcher/watcher.py` — uuid4() in all callbacks.
3. **session_id UUID passed to all events:** `grep "session_id" watcher/watcher.py watcher/main.py` — generated in on_launch, threaded to start(), present in all events.
4. **duration_seconds in death/kill:** `grep "duration_seconds" watcher/watcher.py` — computed from fight_start_time in _on_death, _on_kill, _on_abandon.
5. **fight_abandoned accepted by API:** `grep "fight_abandoned" delu-bot/src/apiServer.js` — in EVENT_HANDLERS map.
6. **durationSeconds forwarded in emitEvent:** `grep "durationSeconds" delu-bot/src/apiServer.js` — in handleBossKill emitEvent call.

If any test fails, fix the issue before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /home/hamza/eldenring-bot && python -m pytest tests/ -v && (cd delu-bot && npx vitest run 2>/dev/null || true)</automated>
  </verify>
  <done>All Python tests pass. All JavaScript tests pass. All 6 phase success criteria are confirmed by code inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability for Phase 5</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/REQUIREMENTS.md` to mark all Phase 5 requirements as complete:

1. Change these checkboxes from `- [ ]` to `- [x]`:
   - COMM-01, COMM-02, COMM-03, COMM-04, COMM-05
   - NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05
   - BET-03
   - STAT-01, STAT-02, STAT-03, STAT-04, STAT-05, STAT-06, STAT-07

2. Update the traceability table at the bottom:
   - Change Status from "Pending" to "Complete" for all 18 requirement IDs listed above

3. Update the `*Last updated:*` line at the bottom to reflect today's date and Phase 5 completion.

**Do NOT change** any other requirements (BET-01, BET-02, BET-04-07, INTG-01-04, COMM-06 remain Pending — those are Phase 6).
  </action>
  <verify>
    <automated>cd /home/hamza/eldenring-bot && grep -c "\[x\]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>All 18 Phase 5 requirement IDs are marked [x] complete in both the requirement list and traceability table. Phase 6 requirements remain Pending.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` — all tests pass
2. JS test suite passes
3. `grep -c "\[x\]" .planning/REQUIREMENTS.md` — count increased by 18 (from DETECT-01-09 + COMM-07 + INTG-05 = 11 previously, now 11+18 = 29)
4. All 6 phase success criteria confirmed
</verification>

<success_criteria>
- Zero test failures across Python and JavaScript suites
- All 6 phase success criteria from ROADMAP confirmed
- REQUIREMENTS.md updated with 18 Phase 5 requirements marked complete
- Phase 6 requirements remain unchanged (Pending)
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-watcher-api-contract-breaks/05-03-SUMMARY.md`
</output>
